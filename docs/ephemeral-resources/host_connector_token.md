---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cloudconnexa_host_connector_token Ephemeral Resource - terraform-provider-cloudconnexa"
subcategory: ""
description: |-
  Mints a setup token for an existing OpenVPN host connector. The token is an ephemeral value: it is never stored in the plan or the state and can only be used in ephemeral contexts such as write-only arguments, provisioner and connection blocks, or other ephemeral resources. A new token is minted every time Terraform opens this resource, which happens once during plan and once during apply, so declare it only where a token is actually consumed. Minting a token does not invalidate tokens minted earlier.
---

# cloudconnexa_host_connector_token (Ephemeral Resource)

Mints a setup token for an existing OpenVPN host connector. The token is an ephemeral value: it is never stored in the plan or the state and can only be used in ephemeral contexts such as write-only arguments, provisioner and connection blocks, or other ephemeral resources. A new token is minted every time Terraform opens this resource, which happens once during plan and once during apply, so declare it only where a token is actually consumed. Minting a token does not invalidate tokens minted earlier.

## Example Usage

```terraform
# Requires Terraform >= 1.10 (ephemeral resources).

variable "app_server_ip" {
  description = "Public IP of the server the connector is installed on."
  type        = string
}

resource "cloudconnexa_host" "app" {
  name = "app-server"
}

resource "cloudconnexa_host_connector" "app" {
  name          = "app-connector"
  host_id       = cloudconnexa_host.app.id
  vpn_region_id = "us-east-1"
}

ephemeral "cloudconnexa_host_connector_token" "app" {
  connector_id = cloudconnexa_host_connector.app.id
}

# Provisioner and connection blocks are ephemeral contexts, so the token can be used to
# bootstrap the host directly without ever being stored.
resource "null_resource" "bootstrap" {
  connection {
    type = "ssh"
    host = var.app_server_ip
    user = "ubuntu"
  }

  provisioner "remote-exec" {
    inline = [
      "sudo openvpn-connector-setup --token '${ephemeral.cloudconnexa_host_connector_token.app.token}'",
    ]
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `connector_id` (String) The ID of the host connector to mint a token for.

### Read-Only

- `token` (String, Sensitive) The setup token, as accepted by `openvpn-connector-setup --token` and the connector deployment templates.
